# VibeCoder 환경변수 예시
# 실제 사용 시 .env 파일로 복사하여 값을 입력하세요

# Flask 시크릿 키 (필수, 강력한 랜덤 문자열)
SECRET_KEY=change-me-to-a-strong-random-string

# PostgreSQL 연결 URL (Render.com 등)
# 비어있으면 SQLite 사용
DATABASE_URL=
